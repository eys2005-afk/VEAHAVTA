# העתק את הקובץ ל-.env ומלא רק את מה שרלוונטי.

# מפתח ה-API של Verbit - נדרש רק אם verbit.mode ב-config.yaml הוא "api"
VERBIT_API_TOKEN=

# מפתח Claude API - נדרש רק לשלב ג' (השוואת פרוטוקול מול תמלול)
ANTHROPIC_API_KEY=
